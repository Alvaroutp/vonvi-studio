# Copia este archivo a .env (en la raíz del repo, junto a docker-compose.yml)
# y pon ahí la contraseña que quieras usar para MySQL dentro de Docker.
# .env NO se sube a Git (agrégalo al .gitignore si no está).
DB_PASSWORD=vonvi123
